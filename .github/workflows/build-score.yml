name: Build e deploy do score.html

on:
  push:
    branches: [main]
    paths:
      - 'score.html'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Confirmar deploy
        run: echo "score.html atualizado — GitHub Pages vai servir automaticamente"
